public class Test1 {
   public static void main(String[] args){
      //  Shapes shape=new Shapes();
      // Circle circle=new Circle();
      // Square square=new Square();
      // Triangle triangle=new Triangle();
      // shape.area();
      // System.out.println(shape.s);
      // // is there exist area() in shape ? yes. then print it.
      //circle.area();
      // // is there exist area() in circle ? yes. then print it.
      // square.area();
      // // is there exist area() in square ? yes. then print it.
      // triangle.area();
      // is there exist area() in triangle ? yes. then print it.
      // Shapes shape=new Shapes();
      Shapes circle=new Circle();
      circle.area();
      System.out.println(circle.s);

      /**ksdjfksdjfklsdjflsdd */  //Documentation comment

      // circle.kalafahad();
      // Shapes square=new Square();
      // Shapes triangle=new Triangle();
      // circle.area();//Area= pi*r*r 
      //this is because overriding for veriable and method is inverse.
   } 
}
